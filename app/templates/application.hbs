<dl>
  {{#each model as |song|}}
    <dt>{{song.name}}</dt>
    <dd>
      <img src="{{song.album.smallArtwork}}">
      {{song.artist.name}} - {{song.album.name}}
    </dd>
  {{/each}}
</dl>

{{outlet}}
